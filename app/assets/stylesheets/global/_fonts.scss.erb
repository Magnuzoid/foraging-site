@font-face {
    font-family: "Lato";
    src:
        local(Lato Thin),
        url(<%= asset_path 'Lato-Hairline.ttf' %> ) format("truetype");
    font-weight: 100;
}
@font-face {
    font-family: "Lato";
    src:
        local(Lato Light),
        url(<%= asset_path 'Lato-Light.ttf' %>) format("truetype");
    font-weight: 300;
}

@font-face {
    font-family: "Lato";
    src:
        local(Lato Regular),
        url(<%= asset_path 'Lato-Regular.ttf' %>) format("truetype");
    font-weight: 400;
}

@font-face {
    font-family: "Lato";
    src:
        local(Lato Bold),
        url(<%= asset_path 'Lato-Bold.ttf' %>) format("truetype");
    font-weight: 700;
}

@font-face {
    font-family: "Lato";
    src:
        local(Lato Black),
        url(<%= asset_path 'Lato-Black.ttf' %>) format("truetype");
    font-weight: 900;
}
